import org.junit.jupiter.api.Test;

public class TestDynamicsArray {
    @Test
    public void test(){
        DynamicsArray dynamicsArray = new DynamicsArray();
        dynamicsArray.addLast(1);
        dynamicsArray.addLast(2);
        dynamicsArray.addLast(3);
        dynamicsArray.addLast(4);
        dynamicsArray.addLast(5);
    }
}
